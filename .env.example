# Copy to .env and set real values for your deployment.
# The env vars are injected at build time and used in the site content.
# Docker Compose env interpolation reference: https://docs.docker.com/compose/how-tos/environment-variables/variable-interpolation/
# Never commit .env. Repo owner should commit only encrypted .env.sops.

VITE_APP_NAME=REPLACE_ME
VITE_MY_NAME=REPLACE_ME
VITE_ROLE=REPLACE_ME
VITE_SUMMARY=REPLACE_ME
VITE_LOCATION=REPLACE_ME
VITE_AVAILABILITY=REPLACE_ME
VITE_PUBLIC_URL=REPLACE_ME
VITE_RESUME_EMBED_URL=REPLACE_ME
VITE_RESUME_DOWNLOAD_URL=REPLACE_ME
PDF_FILENAME=REPLACE_ME.pdf
